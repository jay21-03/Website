import { createContext, useContext, useEffect, useRef, useState } from 'react'
import { useQuery } from '@tanstack/react-query'
import { Link, Navigate, NavLink, Route, Routes, useNavigate, useParams, useSearchParams } from 'react-router-dom'
import { api } from './api'
import Admin from './Admin'
import { validateCheckout } from './domain/checkout'
import { useCheckout, useMyOrder, useMyOrders } from './hooks/useOrders'
import { businessLocalDateTimeToOffset } from './utils/businessDate'
import { formatBusinessDateTime, formatCurrencyVnd } from './utils/format'
import { orderStatusLabel, paymentStatusLabel } from './utils/status'

const Store = createContext(null)
const fallbackImage = '/assets/images/vase.jpg'
const money = formatCurrencyVnd
const date = formatBusinessDateTime
const pick = (lang, vi, en) => lang === 'vi' ? vi : en
const contact = {
  email: 'Cosogombautrucdangxem@gmail.com',
  phones: ['0343478155', '0966477160'],
  facebook: 'https://www.facebook.com/share/18jwSfSPD7/?mibextid=wwXIfr',
  address: '35 Bàu Trúc, thôn Vĩnh Thuận, xã Ninh Phước, tỉnh Khánh Hòa',
  map: 'https://www.google.com/maps/search/?api=1&query=35%20B%C3%A0u%20Tr%C3%BAc%2C%20th%C3%B4n%20V%C4%A9nh%20Thu%E1%BA%ADn%2C%20x%C3%A3%20Ninh%20Ph%C6%B0%E1%BB%9Bc%2C%20t%E1%BB%89nh%20Kh%C3%A1nh%20H%C3%B2a'
}
const supportToContact = settings => settings ? {
  email: settings.email,
  phones: [settings.zaloPhone, settings.secondaryPhone].filter(Boolean),
  facebook: settings.facebookUrl,
  address: settings.address,
  map: settings.mapUrl,
  openingHours: settings.openingHours
} : contact

function StoreProvider({ children }) {
  const [lang, setLang] = useState(() => localStorage.getItem('dxLang') || 'vi')
  const [products, setProducts] = useState([]), [collections, setCollections] = useState([])
  const [cart, setCart] = useState({ items: [], totalAmount: 0 }), [user, setUser] = useState(null)
  const [support, setSupport] = useState(contact)
  const [catalogLoading, setCatalogLoading] = useState(true), [toast, setToast] = useState('')
  const [authLoading, setAuthLoading] = useState(true)
  const notify = message => { setToast(message); window.setTimeout(() => setToast(''), 2500) }
  useEffect(() => {
    Promise.all([api.products(), api.collections()]).then(([page, groups]) => { setProducts(page.content); setCollections(groups) }).catch(error => notify(error.message)).finally(() => setCatalogLoading(false))
    api.supportSettings().then(settings => setSupport(supportToContact(settings))).catch(() => {})
    api.me().then(current => { setUser(current); return current.role === 'USER' ? api.cart() : null }).then(value => value && setCart(value)).catch(() => {}).finally(() => setAuthLoading(false))
  }, [])
  useEffect(() => { localStorage.setItem('dxLang', lang); document.documentElement.lang = lang }, [lang])
  async function add(productId) {
    if (!user) { notify('Vui lòng đăng nhập để thêm sản phẩm.'); return false }
    if (user.role !== 'USER') { notify('Tài khoản quản trị không có giỏ hàng.'); return false }
    try { setCart(await api.addCart(productId)); notify('Đã thêm vào giỏ hàng.'); return true } catch (error) { notify(error.message); return false }
  }
  return <Store.Provider value={{ products, collections, cart, setCart, user, setUser, support, setSupport, catalogLoading, authLoading, add, notify, lang, setLang }}>{children}{toast && <div className="toast show">{toast}</div>}</Store.Provider>
}
const useStore = () => useContext(Store)

function Layout({ children }) {
  const { cart, user, lang, setLang, support } = useStore()
  return <><header className="site-header"><Link className="brand" to="/"><span className="pot-mark">◯</span>Đàng Xem</Link><nav className="main-nav"><NavLink to="/">{pick(lang, 'Trang chủ', 'Home')}</NavLink><NavLink to="/products">{pick(lang, 'Sản phẩm', 'Products')}</NavLink><NavLink to="/collections">{pick(lang, 'Bộ sưu tập', 'Collections')}</NavLink><NavLink to="/workshop">{pick(lang, 'Workshop', 'Workshop')}</NavLink><NavLink to="/support">{pick(lang, 'Hỗ trợ', 'Support')}</NavLink>{user?.role === 'USER' && <NavLink to="/orders">{pick(lang, 'Đơn hàng', 'Orders')}</NavLink>}{user?.role === 'ADMIN' && <NavLink to="/admin">{pick(lang, 'Quản trị', 'Admin')}</NavLink>}</nav><div className="header-tools"><button className="lang-btn" type="button" onClick={() => setLang(lang === 'vi' ? 'en' : 'vi')}>{lang === 'vi' ? 'EN' : 'VI'}</button>{user?.role !== 'ADMIN' && <Link className="icon-btn cart-btn" to="/cart">{pick(lang, 'Giỏ', 'Cart')} <span className="cart-count">{cart.items.reduce((sum, item) => sum + item.quantity, 0)}</span></Link>}{user ? <Link className="icon-btn" to={user.role === 'ADMIN' ? '/admin' : '/account'}>{user.role === 'ADMIN' ? pick(lang, 'Quản trị', 'Admin') : user.fullName}</Link> : <DirectGoogleLogin />}</div></header>{children}<BilingualFooter /><div className="floating-chat">{support.phones[0] && <a className="zalo" href={`https://zalo.me/${support.phones[0]}`} target="_blank" rel="noreferrer" aria-label={`Zalo ${support.phones[0]}`} title={`Zalo ${support.phones[0]}`}>Z</a>}{support.facebook && <a className="facebook" href={support.facebook} target="_blank" rel="noreferrer" aria-label="Facebook" title="Facebook">f</a>}</div></>
}

function DirectGoogleLogin() {
  const { lang, setUser, setCart, notify } = useStore()
  const navigate = useNavigate()
  const [ready, setReady] = useState(Boolean(window.google?.accounts?.id))
  const clientId = import.meta.env.VITE_GOOGLE_CLIENT_ID
  const callbackRef = useRef(null)
  callbackRef.current = async credential => {
    try {
      const result = await api.googleLogin(credential)
      setUser(result.user)
      if (result.user.role === 'USER') setCart(await api.cart())
      notify(pick(lang, 'Đăng nhập thành công.', 'Signed in successfully.'))
      if (result.user.role === 'ADMIN') navigate('/admin', { replace: true })
    } catch (error) { notify(error.message) }
  }
  useEffect(() => {
    if (!clientId) return
    const initialize = () => {
      window.google.accounts.id.initialize({ client_id: clientId, callback: response => callbackRef.current(response.credential), auto_select: false, cancel_on_tap_outside: false })
      setReady(true)
    }
    if (window.google?.accounts?.id) initialize()
    else {
      let script = document.querySelector('script[data-google-identity]')
      if (!script) { script = document.createElement('script'); script.src = 'https://accounts.google.com/gsi/client'; script.async = true; script.dataset.googleIdentity = 'true'; document.head.appendChild(script) }
      script.addEventListener('load', initialize)
      return () => script.removeEventListener('load', initialize)
    }
  }, [clientId])
  function openGoogle() {
    if (!clientId) return notify(pick(lang, 'Chưa cấu hình Google Client ID.', 'Google Client ID is not configured.'))
    if (!ready) return notify(pick(lang, 'Google đang tải, vui lòng thử lại.', 'Google is loading, please try again.'))
    window.google.accounts.id.prompt(notification => {
      if (notification.isNotDisplayed?.() || notification.isSkippedMoment?.()) notify(pick(lang, 'Google không thể mở hộp đăng nhập. Hãy cho phép cookie và popup rồi thử lại.', 'Google could not open sign-in. Allow cookies and popups, then try again.'))
    })
  }
  return <button className="icon-btn direct-login" type="button" onClick={openGoogle}>{pick(lang, 'Đăng nhập', 'Sign in')}</button>
}
function BilingualFooter() {
  const { lang, support } = useStore()
  return <footer className="site-footer"><div className="footer-grid"><div><Link className="brand" to="/">Đàng Xem</Link><p className="footer-slogan">{pick(lang, 'Đất hóa hồn — Tay giữ lửa', 'Earth given soul — Hands guarding fire')}</p><p>{pick(lang, 'Gốm thủ công Chăm Bàu Trúc — Di sản UNESCO 2022.', 'Handcrafted Cham pottery from Bau Truc — UNESCO Heritage 2022.')}</p></div><div className="footer-col"><h3>{pick(lang, 'Liên kết', 'Links')}</h3><Link to="/products">{pick(lang, 'Sản phẩm', 'Products')}</Link><Link to="/workshop">Workshop</Link><Link to="/cart">{pick(lang, 'Giỏ hàng', 'Cart')}</Link><Link to="/orders">{pick(lang, 'Đơn hàng', 'Orders')}</Link><Link to="/support">{pick(lang, 'Hỗ trợ', 'Support')}</Link><Link to="/faq">FAQ</Link><Link to="/policy">{pick(lang, 'Chính sách', 'Policy')}</Link></div><div className="footer-col"><h3>{pick(lang, 'Liên hệ', 'Contact')}</h3><a href={`mailto:${support.email}`}>{support.email}</a>{support.phones.map(phone => <a key={phone} href={`https://zalo.me/${phone}`} target="_blank" rel="noreferrer">Zalo: {phone}</a>)}{support.facebook && <a href={support.facebook} target="_blank" rel="noreferrer">Facebook</a>}{support.map && <a href={support.map} target="_blank" rel="noreferrer">{support.address}</a>}{support.openingHours && <p>{support.openingHours}</p>}</div></div></footer>
}
function Loading({ text = 'Đang tải dữ liệu...' }) { return <div className="state-box">{text}</div> }
function Empty({ children }) { return <div className="state-box">{children}</div> }

function ProductCard({ product, onView }) {
  const { add, lang } = useStore()
  const name = lang === 'vi' ? product.nameVi : product.nameEn
  return <article className="product-card"><Link className="product-image" to={`/products/${product.id}`}><img src={product.thumbnailUrl || fallbackImage} alt={name} onError={event => { event.currentTarget.src = fallbackImage }} /></Link><div className="product-info"><div className="product-top"><div><h3>{name}</h3><p>{lang === 'vi' ? product.nameEn : product.nameVi}</p></div><span className="price">{money(product.sellingPrice, lang)}</span></div>{product.sellingPrice < product.basePrice && <p><s>{money(product.basePrice, lang)}</s></p>}<div className="card-actions"><button onClick={() => onView(product)}>{pick(lang, 'Xem nhanh', 'Quick view')}</button><button className="add-cart" onClick={() => add(product.id)}>{pick(lang, 'Thêm vào giỏ', 'Add to cart')}</button></div></div></article>
}
function ProductModal({ product, onClose }) {
  const { add, lang } = useStore(); if (!product) return null
  const name = lang === 'vi' ? product.nameVi : product.nameEn
  const description = lang === 'vi' ? product.descriptionVi : product.descriptionEn
  return <div className="modal open" onClick={event => event.target === event.currentTarget && onClose()}><div className="modal-card"><button className="close-modal" onClick={onClose}>×</button><div className="zoom-stage"><img src={product.thumbnailUrl || fallbackImage} alt={name} /></div><div className="modal-info"><span className="kicker">{pick(lang, 'Gốm thủ công', 'Handcrafted pottery')}</span><h2>{name}</h2><b>{money(product.sellingPrice, lang)}</b><p>{description || pick(lang, 'Sản phẩm gốm Bàu Trúc được tạo hình hoàn toàn bằng tay.', 'A Bau Truc pottery piece shaped entirely by hand.')}</p><button className="button dark full" onClick={() => add(product.id)}>{pick(lang, 'Thêm vào giỏ hàng', 'Add to cart')}</button></div></div></div>
}
function Home() {
  const { products, catalogLoading, lang } = useStore(); const [view, setView] = useState(null)
  return <Layout><main><section className="hero"><div className="hero-image"><img src="/assets/images/hero-pottery.jpg" alt="Bau Truc pottery artisan" /></div><div className="hero-copy"><span className="kicker">{pick(lang, 'Làng gốm Bàu Trúc · Khánh Hòa', 'Bau Truc Pottery Village · Khanh Hoa')}</span><h1>{pick(lang, <>Đất hóa hồn —<br />Tay giữ lửa</>, <>Earth given soul —<br />Hands guarding fire</>)}</h1><p>{pick(lang, 'Những tác phẩm gốm Chăm độc bản, được tạo hình bằng tay và nung lộ thiên.', 'One-of-a-kind Cham pottery, shaped entirely by hand and fired in the open air.')}</p><Link className="button dark" to="/products">{pick(lang, 'Khám phá sản phẩm →', 'Explore products →')}</Link></div></section><section className="section"><div className="section-head"><div><span className="kicker">{pick(lang, 'Bộ sưu tập', 'Collection')}</span><h2>{pick(lang, 'Sản phẩm nổi bật', 'Featured products')}</h2></div><Link to="/products">{pick(lang, 'Xem tất cả →', 'View all →')}</Link></div>{catalogLoading ? <Loading /> : <div className="product-grid">{products.slice(0, 3).map(product => <ProductCard key={product.id} product={product} onView={setView} />)}</div>}</section></main><ProductModal product={view} onClose={() => setView(null)} /></Layout>
}
function Products() {
  const { collections, lang } = useStore(); const [searchParams] = useSearchParams(); const [filters, setFilters] = useState(() => ({ keyword: searchParams.get('keyword') || '', collectionId: searchParams.get('collectionId') || '', minPrice: searchParams.get('minPrice') || '', maxPrice: searchParams.get('maxPrice') || '', page: Number(searchParams.get('page') || 0), size: 20, sort: searchParams.get('sort') || 'createdAt,desc' })), [view, setView] = useState(null)
  const updateFilter = changes => setFilters(value => ({ ...value, ...changes, page: changes.page ?? 0 }))
  const catalog = useQuery({ queryKey: ['products', filters], queryFn: () => api.products(filters), placeholderData: previous => previous })
  const page = catalog.data
  return <Layout><main className="page"><div className="page-title"><span className="kicker">{pick(lang, 'Cửa hàng', 'Shop')}</span><h1>{pick(lang, 'Tất cả sản phẩm', 'All products')}</h1></div><section className="catalog-layout"><aside className="filters open"><label>{pick(lang, 'Tìm kiếm', 'Search')}</label><input value={filters.keyword} onChange={event => updateFilter({ keyword: event.target.value })} placeholder={pick(lang, 'Tên sản phẩm...', 'Product name...')} /><label>{pick(lang, 'Bộ sưu tập', 'Collection')}</label><select value={filters.collectionId} onChange={event => updateFilter({ collectionId: event.target.value })}><option value="">{pick(lang, 'Tất cả', 'All')}</option>{collections.map(item => <option key={item.id} value={item.id}>{lang === 'vi' ? item.nameVi : item.nameEn}</option>)}</select><label>{pick(lang, 'Giá từ', 'Min price')}</label><input inputMode="numeric" value={filters.minPrice} onChange={event => updateFilter({ minPrice: event.target.value.replace(/\D/g, '') })} /><label>{pick(lang, 'Giá đến', 'Max price')}</label><input inputMode="numeric" value={filters.maxPrice} onChange={event => updateFilter({ maxPrice: event.target.value.replace(/\D/g, '') })} /><label>{pick(lang, 'Sắp xếp', 'Sort')}</label><select value={filters.sort} onChange={event => updateFilter({ sort: event.target.value })}><option value="createdAt,desc">{pick(lang, 'Mới nhất', 'Newest')}</option><option value="sellingPrice,asc">{pick(lang, 'Giá thấp → cao', 'Price low → high')}</option><option value="sellingPrice,desc">{pick(lang, 'Giá cao → thấp', 'Price high → low')}</option><option value="nameVi,asc">{pick(lang, 'Tên A → Z', 'Name A → Z')}</option></select></aside><div>{catalog.isLoading ? <Loading /> : catalog.isError ? <div className="state-box error" role="alert">{catalog.error.message}<button onClick={() => catalog.refetch()}>{pick(lang, 'Thử lại', 'Retry')}</button></div> : !page?.content.length ? <Empty>{pick(lang, 'Không tìm thấy sản phẩm phù hợp.', 'No matching products found.')}</Empty> : <><div className="results-bar"><span>{page.totalElements} {pick(lang, 'sản phẩm', 'products')}</span>{catalog.isFetching && <span>{pick(lang, 'Đang cập nhật...', 'Refreshing...')}</span>}</div><div className="product-grid">{page.content.map(product => <ProductCard key={product.id} product={product} onView={setView} />)}</div>{page.totalPages > 1 && <nav className="pagination" aria-label="Phân trang sản phẩm"><button disabled={page.first || catalog.isFetching} onClick={() => updateFilter({ page: page.page - 1 })}>{pick(lang, 'Trang trước', 'Previous')}</button><span>Trang {page.page + 1}/{page.totalPages}</span><button disabled={page.last || catalog.isFetching} onClick={() => updateFilter({ page: page.page + 1 })}>{pick(lang, 'Trang sau', 'Next')}</button></nav>}</>}</div></section></main><ProductModal product={view} onClose={() => setView(null)} /></Layout>
}
function Account() {
  const { user, setUser, setCart, lang } = useStore(); const navigate = useNavigate()
  async function logout() { try { await api.logout() } finally { setUser(null); setCart({ items: [], totalAmount: 0 }); navigate('/') } }
  if (!user) return <Navigate to="/" replace />
  return <Layout><main className="auth-page"><section className="auth-card"><span className="account-avatar">{user.fullName?.charAt(0)}</span><span className="kicker">{pick(lang, 'Tài khoản', 'Account')}</span><h1>{pick(lang, 'Xin chào', 'Hello')}, {user.fullName}</h1><p>{user.email}</p><div className="account-actions">{user.role === 'ADMIN' ? <Link className="button dark" to="/admin">{pick(lang, 'Vào quản trị', 'Open admin')}</Link> : <Link className="button dark" to="/orders">{pick(lang, 'Xem đơn hàng', 'View orders')}</Link>}<button className="button light" onClick={logout}>{pick(lang, 'Đăng xuất', 'Sign out')}</button></div></section></main></Layout>
}
function CollectionsPage() {
  const { collections, catalogLoading, lang } = useStore()
  return <Layout><main className="page"><div className="page-title"><span className="kicker">{pick(lang, 'Bộ sưu tập', 'Collections')}</span><h1>{pick(lang, 'Khám phá theo bộ sưu tập', 'Explore by collection')}</h1></div>{catalogLoading ? <Loading /> : !collections.length ? <Empty>{pick(lang, 'Chưa có bộ sưu tập đang hiển thị.', 'No active collections are available.')}</Empty> : <section className="collection-grid">{collections.map(item => <article className="collection-card" key={item.id}><h2>{lang === 'vi' ? item.nameVi : item.nameEn}</h2><p>{lang === 'vi' ? item.descriptionVi : item.descriptionEn}</p><Link className="button light full" to={`/products?collectionId=${item.id}`}>{pick(lang, 'Xem sản phẩm', 'View products')}</Link></article>)}</section>}</main></Layout>
}

function ProductDetail() {
  const { id } = useParams(); const { add, lang } = useStore(); const [selected, setSelected] = useState(0); const [zoom, setZoom] = useState(1); const [touchStart, setTouchStart] = useState(null); const productQuery = useQuery({ queryKey: ['product', id], queryFn: () => api.product(id), enabled: Boolean(id) }); const product = productQuery.data
  const images = product?.images?.length ? product.images : [{ id: 'fallback', url: product?.thumbnailUrl || fallbackImage, thumbnail: true }]
  const activeImage = images[Math.min(selected, images.length - 1)] || images[0]
  const selectImage = index => { setSelected(index); setZoom(1) }
  const moveImage = direction => selectImage((selected + direction + images.length) % images.length)
  const handleTouchEnd = event => {
    if (touchStart == null || images.length < 2) return
    const delta = event.changedTouches[0].clientX - touchStart
    if (Math.abs(delta) > 40) moveImage(delta < 0 ? 1 : -1)
    setTouchStart(null)
  }
  if (productQuery.isLoading) return <Layout><main className="page"><Loading /></main></Layout>
  if (productQuery.isError) return <Layout><main className="page"><div className="state-box error" role="alert">{productQuery.error.message}<button onClick={() => productQuery.refetch()}>Thử lại</button></div></main></Layout>
  if (!product) return <Layout><main className="page"><Empty>Không tìm thấy sản phẩm.</Empty></main></Layout>
  const name = lang === 'vi' ? product.nameVi : product.nameEn
  const description = lang === 'vi' ? product.descriptionVi : product.descriptionEn
  return <Layout><main className="page product-detail-page"><div className="product-detail-layout"><section><div className="product-detail-image" onTouchStart={event => setTouchStart(event.changedTouches[0].clientX)} onTouchEnd={handleTouchEnd}><img style={{ transform: `scale(${zoom})` }} src={activeImage?.url || fallbackImage} alt={name} onError={event => { event.currentTarget.src = fallbackImage }} /></div><div className="gallery-controls"><button type="button" aria-label={pick(lang, 'Thu nhỏ ảnh', 'Zoom out')} disabled={zoom <= 1} onClick={() => setZoom(value => Math.max(1, Number((value - 0.25).toFixed(2))))}>−</button><span>{Math.round(zoom * 100)}%</span><button type="button" aria-label={pick(lang, 'Phóng to ảnh', 'Zoom in')} disabled={zoom >= 2} onClick={() => setZoom(value => Math.min(2, Number((value + 0.25).toFixed(2))))}>+</button></div>{images.length > 1 && <><div className="gallery-step"><button type="button" aria-label={pick(lang, 'Ảnh trước', 'Previous image')} onClick={() => moveImage(-1)}>‹</button><button type="button" aria-label={pick(lang, 'Ảnh sau', 'Next image')} onClick={() => moveImage(1)}>›</button></div><div className="product-thumbs">{images.map((image, index) => <button key={image.id} className={index === selected ? 'active' : ''} onClick={() => selectImage(index)} aria-label={`${pick(lang, 'Chọn ảnh', 'Select image')} ${index + 1}`}><img src={image.url} alt="" /></button>)}</div></>}</section><section className="product-detail-info"><span className="kicker">{pick(lang, 'Chi tiết sản phẩm', 'Product detail')}</span><h1>{name}</h1><p>{description || pick(lang, 'Sản phẩm gốm Bàu Trúc được tạo hình thủ công.', 'A handcrafted Bau Truc pottery piece.')}</p><b className="detail-price">{money(product.sellingPrice, lang)}</b>{product.sellingPrice < product.basePrice && <p><s>{money(product.basePrice, lang)}</s></p>}<button className="button dark full" onClick={() => add(product.id)}>{pick(lang, 'Thêm vào giỏ hàng', 'Add to cart')}</button><Link className="button light full" to="/products">{pick(lang, 'Quay lại danh sách', 'Back to products')}</Link></section></div></main></Layout>
}
function Cart() {
  const { cart, setCart, user, notify, lang, authLoading } = useStore()
  async function change(item, quantity) { try { setCart(quantity < 1 ? await api.removeCart(item.id) : await api.updateCart(item.id, quantity)) } catch (error) { notify(error.message) } }
  return <Layout><main className="page"><div className="page-title"><span className="kicker">{pick(lang, 'Giỏ hàng', 'Cart')}</span><h1>{pick(lang, 'Giỏ hàng của bạn', 'Your cart')}</h1></div>{authLoading ? <Loading /> : !user || user.role !== 'USER' ? <Empty>{pick(lang, 'Bạn cần đăng nhập bằng tài khoản khách hàng để dùng giỏ hàng.', 'Sign in with a customer account to use the cart.')}</Empty> : !cart.items.length ? <Empty>{pick(lang, 'Giỏ hàng đang trống.', 'Your cart is empty.')} <Link to="/products">{pick(lang, 'Tiếp tục mua sắm', 'Continue shopping')}</Link></Empty> : <div className="checkout-grid"><section className="order-summary cart-panel">{cart.items.map(item => <div className="checkout-item" key={item.id}><img src={item.thumbnailUrl || fallbackImage} alt="" /><div><b>{lang === 'vi' ? item.nameVi : item.nameEn}</b><span>{pick(lang, 'Còn có thể đặt', 'Available to order')}: {item.availableQuantity}</span><span><button type="button" onClick={() => change(item, item.quantity - 1)}>−</button> {item.quantity} <button type="button" disabled={item.quantity >= item.availableQuantity} onClick={() => change(item, item.quantity + 1)}>+</button></span><button type="button" className="link-danger" onClick={() => change(item, 0)}>{pick(lang, 'Xóa', 'Remove')}</button></div><div><small>{money(item.sellingPrice, lang)} × {item.quantity}</small><b>{money(item.lineTotal, lang)}</b></div></div>)}<div className="summary-line"><span>{pick(lang, 'Tạm tính', 'Subtotal')}</span><b>{money(cart.totalAmount, lang)}</b></div><div className="summary-line total"><span>{pick(lang, 'Tổng cộng', 'Total')}</span><b>{money(cart.totalAmount, lang)}</b></div><Link className="button dark full" to="/checkout">{pick(lang, 'Tiếp tục thanh toán', 'Continue to checkout')}</Link></section></div>}</main></Layout>
}
function Checkout() {
  const { cart, setCart, user, notify, lang, authLoading } = useStore(); const [errors, setErrors] = useState({}); const [submitError, setSubmitError] = useState(''); const navigate = useNavigate(); const checkout = useCheckout()
  async function change(item, quantity) { try { setCart(quantity < 1 ? await api.removeCart(item.id) : await api.updateCart(item.id, quantity)) } catch (error) { notify(error.message) } }
  async function submit(event) { event.preventDefault(); if (!user) return navigate('/account'); if (checkout.isPending) return; const result = validateCheckout(Object.fromEntries(new FormData(event.currentTarget))); setErrors(result.errors); setSubmitError(''); if (!result.data) return; try { const created = await checkout.mutateAsync(result.data); setCart({ items: [], totalAmount: 0 }); navigate(`/orders/${created.orderId}`, { replace: true }) } catch (error) { setSubmitError(error.message); if ([400, 409].includes(error.status)) { try { setCart(await api.cart()) } catch { /* preserve the checkout error */ } } } }
  const field = (name, label, control) => <label className={['email', 'address', 'note'].includes(name) ? 'wide' : ''}>{label}{control}{errors[name] && <small className="field-error" role="alert">{errors[name]}</small>}</label>
  return <Layout><main className="page checkout-page"><div className="page-title"><span className="kicker">{pick(lang, 'Giỏ hàng', 'Cart')}</span><h1>{pick(lang, 'Thông tin đặt hàng', 'Checkout details')}</h1></div>{authLoading ? <Loading text={pick(lang, 'Đang kiểm tra tài khoản...', 'Checking account...')} /> : !user || user.role !== 'USER' ? <Empty>{pick(lang, 'Bạn cần đăng nhập bằng tài khoản khách hàng để đặt hàng.', 'Sign in with a customer account to checkout.')}</Empty> : !cart.items.length ? <Empty>{pick(lang, 'Giỏ hàng đang trống.', 'Your cart is empty.')} <Link to="/products">{pick(lang, 'Tiếp tục mua sắm', 'Continue shopping')}</Link></Empty> : <div className="checkout-grid"><form className="checkout-form" onSubmit={submit} noValidate><h3>{pick(lang, 'Thông tin nhận hàng', 'Recipient information')}</h3>{submitError && <div className="form-error" role="alert">{submitError}</div>}<div className="form-grid">{field('receiverName', pick(lang, 'Họ và tên', 'Full name'), <input name="receiverName" defaultValue={user.fullName} maxLength="255" aria-invalid={Boolean(errors.receiverName)} />)}{field('phone', pick(lang, 'Số điện thoại', 'Phone number'), <input name="phone" maxLength="32" aria-invalid={Boolean(errors.phone)} />)}{field('email', 'Email', <input name="email" type="email" defaultValue={user.email} maxLength="320" aria-invalid={Boolean(errors.email)} />)}{field('address', pick(lang, 'Địa chỉ', 'Address'), <input name="address" maxLength="500" aria-invalid={Boolean(errors.address)} />)}{field('note', pick(lang, 'Ghi chú', 'Note'), <textarea name="note" maxLength="1000" aria-invalid={Boolean(errors.note)} />)}</div><button className="button dark full" disabled={checkout.isPending}>{checkout.isPending ? pick(lang, 'Đang tạo đơn hàng...', 'Creating order...') : `${pick(lang, 'Đặt hàng', 'Place order')} ${money(cart.totalAmount, lang)}`}</button></form><aside className="order-summary"><h3>{pick(lang, 'Đơn hàng của bạn', 'Your order')}</h3>{cart.items.map(item => <div className="checkout-item" key={item.id}><img src={item.thumbnailUrl || fallbackImage} alt="" /><div><b>{lang === 'vi' ? item.nameVi : item.nameEn}</b><span><button type="button" aria-label={pick(lang, 'Giảm số lượng', 'Decrease quantity')} disabled={checkout.isPending} onClick={() => change(item, item.quantity - 1)}>−</button> {item.quantity} <button type="button" aria-label={pick(lang, 'Tăng số lượng', 'Increase quantity')} disabled={checkout.isPending || item.quantity >= item.availableQuantity} onClick={() => change(item, item.quantity + 1)}>+</button></span><button type="button" className="link-danger" disabled={checkout.isPending} onClick={() => change(item, 0)}>{pick(lang, 'Xóa', 'Remove')}</button></div><div><small>{money(item.sellingPrice, lang)} × {item.quantity}</small><b>{money(item.lineTotal, lang)}</b></div></div>)}<div className="summary-line total"><span>{pick(lang, 'Tổng cộng', 'Total')}</span><b>{money(cart.totalAmount, lang)}</b></div></aside></div>}</main></Layout>
}
function Orders() {
  const { user, lang, authLoading } = useStore(); const [pageNumber, setPageNumber] = useState(0); const orders = useMyOrders(pageNumber, user?.role === 'USER'); const page = orders.data
  return <Layout><main className="page"><div className="page-title"><span className="kicker">{pick(lang, 'Tài khoản', 'Account')}</span><h1>{pick(lang, 'Đơn hàng của tôi', 'My orders')}</h1></div>{authLoading || orders.isLoading ? <Loading /> : !user || user.role !== 'USER' ? <Empty>{pick(lang, 'Vui lòng đăng nhập bằng tài khoản khách hàng.', 'Please sign in with a customer account.')}</Empty> : orders.isError ? <div className="state-box error" role="alert">{orders.error.message}<button onClick={() => orders.refetch()}>{pick(lang, 'Thử lại', 'Retry')}</button></div> : !page?.content.length ? <Empty>{pick(lang, 'Bạn chưa có đơn hàng.', 'You do not have any orders yet.')}</Empty> : <><div className="order-list">{page.content.map(order => <Link className="order-row" to={`/orders/${order.id}`} key={order.id}><div><b>{order.orderCode}</b><small>{date(order.createdAt, lang)}</small></div><span>{pick(lang, 'Đơn hàng', 'Order')}: {orderStatusLabel(order.orderStatus, lang)}</span><span>{pick(lang, 'Thanh toán', 'Payment')}: {paymentStatusLabel(order.paymentStatus, lang)}</span><b>{money(order.totalAmount, lang)}</b></Link>)}</div>{page.totalPages > 1 && <nav className="pagination" aria-label={pick(lang, 'Phân trang đơn hàng', 'Order pagination')}><button disabled={page.first} onClick={() => setPageNumber(value => value - 1)}>{pick(lang, 'Trang trước', 'Previous')}</button><span>{pick(lang, 'Trang', 'Page')} {page.page + 1}/{page.totalPages}</span><button disabled={page.last} onClick={() => setPageNumber(value => value + 1)}>{pick(lang, 'Trang sau', 'Next')}</button></nav>}</>}</main></Layout>
}
function OrderDetail() {
  const { id } = useParams(); const { user, lang, authLoading } = useStore(); const query = useMyOrder(id, user?.role === 'USER'); const order = query.data
  const pendingPayment = order?.paymentStatus === 'PENDING'
  return <Layout><main className="page"><div className="page-title"><span className="kicker">{pick(lang, 'Chi tiết đơn hàng', 'Order detail')}</span><h1>{order?.orderCode || pick(lang, 'Đơn hàng', 'Order')}</h1></div>{authLoading || query.isLoading ? <Loading /> : !user || user.role !== 'USER' ? <Empty>{pick(lang, 'Vui lòng đăng nhập bằng tài khoản khách hàng.', 'Please sign in with a customer account.')}</Empty> : query.isError && query.error.status === 404 ? <Empty>{pick(lang, 'Không tìm thấy đơn hàng.', 'Order not found.')}</Empty> : query.isError ? <div className="state-box error" role="alert">{query.error.message}<button onClick={() => query.refetch()}>{pick(lang, 'Thử lại', 'Retry')}</button></div> : <div className="order-detail"><section><h3>{pick(lang, 'Thông tin nhận hàng', 'Recipient information')}</h3><p>{order.receiverName} · {order.phone}</p>{order.email && <p>{order.email}</p>}<p>{order.address}</p>{order.note && <p>{pick(lang, 'Ghi chú', 'Note')}: {order.note}</p>}<p>{pick(lang, 'Ngày tạo', 'Created at')}: {date(order.createdAt, lang)}</p><p>{pick(lang, 'Đơn hàng', 'Order')}: <b>{orderStatusLabel(order.orderStatus, lang)}</b></p><p>{pick(lang, 'Thanh toán', 'Payment')}: <b>{paymentStatusLabel(order.paymentStatus, lang)}</b></p>{pendingPayment && <small>{pick(lang, 'Trang này tự cập nhật trạng thái thanh toán mỗi 5 giây.', 'This page refreshes payment status every 5 seconds.')}</small>}<div className="payment-panel"><h3>{pick(lang, 'Thanh toán payOS', 'payOS payment')}</h3><div className="summary-line"><span>{pick(lang, 'Trạng thái', 'Status')}</span><b>{paymentStatusLabel(order.paymentStatus, lang)}</b></div><div className="summary-line"><span>{pick(lang, 'Số tiền thanh toán', 'Payment amount')}</span><b>{money(order.paymentAmount ?? order.totalAmount, lang)}</b></div>{order.expiresAt && <div className="summary-line"><span>{pick(lang, 'Hết hạn', 'Expires at')}</span><b>{date(order.expiresAt, lang)}</b></div>}{order.qrCode && <img className="payment-qr" src={order.qrCode} alt={pick(lang, 'Mã QR thanh toán payOS', 'payOS payment QR code')} />}{order.checkoutUrl ? <a className="button dark full" href={order.checkoutUrl} target="_blank" rel="noreferrer">{pick(lang, 'Thanh toán qua payOS', 'Pay with payOS')}</a> : pendingPayment && <p className="payment-note">{pick(lang, 'Cổng thanh toán đang chờ cấu hình hoặc thông tin thanh toán chưa khả dụng. Trạng thái đơn hàng vẫn được lấy từ hệ thống.', 'Payment gateway configuration or payment details are not available yet. Order state remains system-authoritative.')}</p>}</div></section><aside className="order-summary">{order.items.map(item => <div className="summary-line" key={item.productId}><span>{lang === 'vi' ? item.productNameVi : item.productNameEn} × {item.quantity}<small>{money(item.sellingPrice, lang)} / {pick(lang, 'sản phẩm', 'item')}</small></span><b>{money(item.totalPrice, lang)}</b></div>)}<div className="summary-line"><span>{pick(lang, 'Tạm tính', 'Subtotal')}</span><b>{money(order.subtotal, lang)}</b></div><div className="summary-line total"><span>{pick(lang, 'Tổng cộng', 'Total')}</span><b>{money(order.totalAmount, lang)}</b></div></aside></div>}</main></Layout>
}
function Faq() { const { lang } = useStore(); return <Layout><main className="page support-page"><div className="page-title"><span className="kicker">FAQ</span><h1>{pick(lang, 'Câu hỏi thường gặp', 'Frequently asked questions')}</h1></div><section className="faq-list"><details open><summary>{pick(lang, 'Gốm Bàu Trúc có dùng bàn xoay không?', 'Is Bau Truc pottery made with a wheel?')}</summary><p>{pick(lang, 'Không. Sản phẩm được tạo hình bằng tay theo kỹ thuật truyền thống của người Chăm.', 'No. Products are shaped by hand using traditional Cham techniques.')}</p></details><details><summary>{pick(lang, 'Tôi có thể đặt lịch workshop không?', 'Can I book a workshop?')}</summary><p>{pick(lang, 'Có. Mở trang Workshop, chọn gói phù hợp và gửi yêu cầu đặt lịch để cửa hàng xác nhận.', 'Yes. Open Workshop, choose an offering, and submit a booking request for store confirmation.')}</p></details><details><summary>{pick(lang, 'Thanh toán online hoạt động thế nào?', 'How does online payment work?')}</summary><p>{pick(lang, 'Hệ thống tạo Payment trạng thái PENDING và hiển thị thông tin payOS nếu cổng thanh toán đã được cấu hình.', 'The system creates a PENDING payment and shows payOS details when the gateway is configured.')}</p></details></section></main></Layout> }
function Policy() { const { lang } = useStore(); return <Layout><main className="page support-page"><div className="page-title"><span className="kicker">{pick(lang, 'Chính sách', 'Policy')}</span><h1>{pick(lang, 'Chính sách mua hàng', 'Purchase policy')}</h1></div><section className="policy"><h2>{pick(lang, 'Thanh toán', 'Payment')}</h2><p>{pick(lang, 'Đơn hàng sử dụng thanh toán payOS. Ứng dụng không tự đánh dấu thanh toán thành công; trạng thái được cập nhật từ hệ thống backend.', 'Orders use payOS payment. The app does not mark payment success by itself; status is updated by the backend system.')}</p><h2>{pick(lang, 'Hoàn tiền thủ công', 'Manual refund')}</h2><p>{pick(lang, 'Hoàn tiền tự động ngoài phạm vi. Khi cần, admin chỉ ghi nhận khoản hoàn tiền đã thực hiện bên ngoài hệ thống.', 'Automatic refund is out of scope. When needed, admins only record refunds already completed outside the system.')}</p><h2>{pick(lang, 'Vận chuyển', 'Shipping')}</h2><p>{pick(lang, 'Phí vận chuyển và theo dõi vận chuyển chưa thuộc phạm vi hệ thống hiện tại.', 'Shipping fee and shipment tracking are outside the current system scope.')}</p></section></main></Layout> }
function Workshop() {
  const { user, notify, lang, support } = useStore()
  const [workshops, setWorkshops] = useState([])
  const [selectedId, setSelectedId] = useState('')
  const [submitting, setSubmitting] = useState(false)
  const [created, setCreated] = useState(null)
  useEffect(() => { api.workshops().then(setWorkshops).catch(error => notify(error.message)) }, [notify])
  const defaultName = user?.fullName || ''
  const defaultEmail = user?.email || ''
  async function submit(event) {
    event.preventDefault()
    if (submitting) return
    const values = Object.fromEntries(new FormData(event.currentTarget))
    values.workshopId = values.workshopId ? Number(values.workshopId) : null
    values.participants = Number(values.participants)
    values.preferredAt = businessLocalDateTimeToOffset(values.preferredAt)
    setSubmitting(true)
    try {
      const result = await api.workshopBooking(values)
      setCreated(result)
      event.currentTarget.reset()
      notify(pick(lang, 'Đã gửi yêu cầu đặt lịch workshop.', 'Workshop booking request sent.'))
    } catch (error) { notify(error.message) } finally { setSubmitting(false) }
  }
  return <Layout><main className="page workshop-page"><section className="workshop-hero"><div><span className="kicker">Workshop Bàu Trúc</span><h1>{pick(lang, 'Đặt lịch trải nghiệm làm gốm', 'Book a pottery workshop')}</h1><p>{pick(lang, 'Trải nghiệm tạo hình gốm Chăm bằng tay, tìm hiểu đất sét Bàu Trúc và quy trình nung lộ thiên truyền thống.', 'Shape Cham pottery by hand, learn about Bau Truc clay, and explore the traditional open-firing process.')}</p><div className="workshop-facts"><span>{pick(lang, 'Gói trải nghiệm rõ giá', 'Transparent offering prices')}</span><span>{pick(lang, 'Đặt lịch linh hoạt', 'Flexible scheduling')}</span><span>{pick(lang, 'Xác nhận thủ công', 'Manual confirmation')}</span></div></div><img src="/assets/images/artisan.jpg" alt={pick(lang, 'Nghệ nhân gốm Bàu Trúc', 'Bau Truc pottery artisan')} /></section>{workshops.length > 0 && <section className="workshop-cards">{workshops.map(item => <article className={String(item.id) === String(selectedId) ? 'selected' : ''} key={item.id}><img src={item.imageUrl || '/assets/images/tour.jpg'} alt={item.title} /><div><h2>{item.title}</h2><p>{item.description}</p><b>{money(item.priceAmount, lang)}</b><span>{item.durationMinutes} {pick(lang, 'phút', 'minutes')} · {pick(lang, 'tối đa', 'up to')} {item.maxParticipants} {pick(lang, 'khách', 'guests')}</span><button className="button light full" onClick={() => setSelectedId(String(item.id))}>{pick(lang, 'Chọn gói này', 'Select this offering')}</button></div></article>)}</section>}<section className="workshop-layout"><form className="workshop-form" onSubmit={submit}><h2>{pick(lang, 'Thông tin đặt lịch', 'Booking information')}</h2>{created && <div className="form-success" role="status">{pick(lang, `Yêu cầu #${created.id} đã được ghi nhận. Cửa hàng sẽ liên hệ xác nhận.`, `Request #${created.id} has been recorded. The store will contact you to confirm.`)}</div>}<label className="wide">{pick(lang, 'Gói workshop', 'Workshop offering')}<select name="workshopId" value={selectedId} onChange={e => setSelectedId(e.target.value)}><option value="">{pick(lang, 'Tư vấn chọn gói', 'Help me choose')}</option>{workshops.map(item => <option key={item.id} value={item.id}>{item.title} · {money(item.priceAmount, lang)}</option>)}</select></label><label>{pick(lang, 'Họ và tên', 'Full name')}<input name="fullName" defaultValue={defaultName} maxLength="255" required /></label><label>Email<input name="email" type="email" defaultValue={defaultEmail} maxLength="320" required /></label><label>{pick(lang, 'Số điện thoại', 'Phone number')}<input name="phone" maxLength="32" required /></label><label>{pick(lang, 'Ngày giờ mong muốn', 'Preferred date and time')}<input name="preferredAt" type="datetime-local" required /></label><label>{pick(lang, 'Số người', 'Participants')}<input name="participants" type="number" min="1" max="30" defaultValue="2" required /></label><label>{pick(lang, 'Ghi chú', 'Note')}<textarea name="note" maxLength="1000" placeholder={pick(lang, 'Ví dụ: có trẻ em đi cùng, cần tư vấn gói trải nghiệm...', 'Example: children joining, need help choosing an offering...')} /></label><button className="button dark full" disabled={submitting}>{submitting ? pick(lang, 'Đang gửi...', 'Sending...') : pick(lang, 'Gửi yêu cầu đặt lịch', 'Submit booking request')}</button></form><aside className="workshop-info"><h2>{pick(lang, 'Thông tin trải nghiệm', 'Experience information')}</h2><p><b>{pick(lang, 'Địa điểm', 'Location')}:</b> {support.address}</p><p><b>{pick(lang, 'Liên hệ', 'Contact')}:</b> {support.phones.join(' · ')}</p>{support.openingHours && <p><b>{pick(lang, 'Giờ mở cửa', 'Opening hours')}:</b> {support.openingHours}</p>}<p><b>{pick(lang, 'Lưu ý', 'Note')}:</b> {pick(lang, 'Đây là yêu cầu đặt lịch. Admin sẽ xác nhận lại thời gian qua điện thoại hoặc email.', 'This is a booking request. Admin will confirm the time by phone or email.')}</p><Link className="button light full" to="/support">{pick(lang, 'Cần tư vấn thêm', 'Need more help')}</Link></aside></section></main></Layout>
}
function Support() { const { lang, support } = useStore(); return <Layout><main className="page support-page"><div className="page-title"><span className="kicker">{pick(lang, 'Hỗ trợ', 'Support')}</span><h1>{pick(lang, 'Liên hệ với chúng tôi', 'Contact us')}</h1></div><div className="support-layout"><section className="contact-card"><h2>{pick(lang, 'Cơ sở gốm Bàu Trúc Đàng Xem', 'Dang Xem Bau Truc Pottery')}</h2><p><b>Email:</b> <a href={`mailto:${support.email}`}>{support.email}</a></p><p><b>Zalo:</b> {support.phones.map((phone, index) => <span key={phone}>{index > 0 && ' · '}<a href={`https://zalo.me/${phone}`} target="_blank" rel="noreferrer">{phone}</a></span>)}</p>{support.facebook && <p><b>Facebook:</b> <a href={support.facebook} target="_blank" rel="noreferrer">{pick(lang, 'Xem trang Facebook', 'Open Facebook page')}</a></p>}<p><b>{pick(lang, 'Địa chỉ', 'Address')}:</b> {support.map ? <a href={support.map} target="_blank" rel="noreferrer">{support.address}</a> : support.address}</p>{support.openingHours && <p><b>{pick(lang, 'Giờ mở cửa', 'Opening hours')}:</b> {support.openingHours}</p>}</section><section className="faq-list"><details open><summary>{pick(lang, 'Gốm Bàu Trúc có dùng bàn xoay không?', 'Is Bau Truc pottery made with a wheel?')}</summary><p>{pick(lang, 'Không. Sản phẩm được tạo hình hoàn toàn bằng tay theo kỹ thuật truyền thống của người Chăm.', 'No. Every piece is shaped entirely by hand using traditional Cham techniques.')}</p></details><details><summary>{pick(lang, 'Làm sao theo dõi đơn hàng?', 'How can I track an order?')}</summary><p>{pick(lang, 'Đăng nhập và mở mục Đơn hàng trên thanh điều hướng.', 'Sign in and open Orders in the navigation bar.')}</p></details></section></div></main></Layout> }

function AdminPortal() {
  const store = useStore()
  async function logout() { try { await api.logout() } finally { store.setUser(null); window.location.assign('/') } }
  if (store.authLoading) return <div className="admin-state">{pick(store.lang, 'Đang xác thực quyền quản trị...', 'Checking admin access...')}</div>
  return <Admin user={store.user} notify={store.notify} onLogout={logout} lang={store.lang} />
}

export default function App() { return <StoreProvider><Routes><Route path="/" element={<Home />} /><Route path="/products" element={<Products />} /><Route path="/products/:id" element={<ProductDetail />} /><Route path="/collections" element={<CollectionsPage />} /><Route path="/workshop" element={<Workshop />} /><Route path="/login" element={<Account />} /><Route path="/account" element={<Account />} /><Route path="/cart" element={<Cart />} /><Route path="/checkout" element={<Checkout />} /><Route path="/orders" element={<Orders />} /><Route path="/orders/:id" element={<OrderDetail />} /><Route path="/support" element={<Support />} /><Route path="/faq" element={<Faq />} /><Route path="/policy" element={<Policy />} /><Route path="/admin" element={<AdminPortal />} /><Route path="*" element={<Home />} /></Routes></StoreProvider> }
